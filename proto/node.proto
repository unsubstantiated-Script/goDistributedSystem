syntax = "proto3";

package core;

option go_package = "distributed-system/pkg/pb;pb";

message Request {
  string data = 1;
}

message Response {
  string data = 1;
}

service NodeService {
  rpc ReportStatus(Request) returns (Response);
  rpc AssignTask(Request) returns (stream Response);
}
